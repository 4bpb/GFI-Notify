One way of getting around having users copy the code into smartthings

will throw an error if code is not correct
I wouldnt suggest using this since it is error prone, using reqeuests wouldve been better and faster.